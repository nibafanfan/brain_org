# Worklog — 2026-05-25 session

Durable record of what this terminal session did + key results, in case the
terminal closes. Companion to `v5_outcomes_and_validation.md` (formal results)
and `rebuild_plan_2026-05-23.md` (plan).

## TL;DR
Built/annotated/preprocessed/scVI-trained **atlas v5**, validated it, pushed code
to GitHub, reconciled a Codex methods review, ran HVG 3k-vs-5k + a rare-lineage
diagnosis, and ran the **full Braun→scANVI label transfer — which hit a classifier
collapse** (100% Radial glia). The transfer **latent is good**; the fix (kNN-on-
latent CellClass) is pending.

---

## 1. Disk cleanup (~128 GB freed)
Deleted obsolete `_archive/atlas_v1/v2/v3*` builds + the broken 0-gene
`atlas_v5_full.h5ad`. Free space 223 GB → 351 GB. All data deletions verified as
recoverable/duplicates first.

## 2. Atlas v5 build → `data/atlas_v5_full.h5ad`
- **4,079,890 cells × 36,842 genes, 118 deposits, 500 GSMs.** Control-only;
  excludes HNOCA-overlap `gse168323`; includes HTO-fixed `gse297594` (47,844 WT).
- **Reused 6 deposits** (gse119861/145306/157019/163018/164089/171263, 86,643
  cells) via `reuse_from_archive.py` after fixing stale `gsms` in
  `rebuild_config.tsv` (backup `rebuild_config.tsv.bak_pre_reuse6`). Sanity-checked:
  no dup barcodes, all control.
- **Two concat bugs fixed** in `concatenate_atlas.py`: (a) `concat_on_disk(merge=
  None)` silently drops obs columns not present in ALL deposits; (b) `multi_lineage`
  bool-vs-categorical dtype crash. Added a post-concat **0-gene guard**.

## 3. Annotation (`inject_finalized_annotations.py`, `recover_per_cell_annotations.py`)
- 8 finalized fields per cell, keyed by GSM, **fill-don't-clobber**, with an
  `annotation_level` flag. **100% value-accurate vs the sheet**; **94.5% gsm-level**.
- Per-cell recovery from barcodes for `gse281535` (timepoint), `gse227640`
  (line/condition/week), `gse195690` (+132,809 cells to gsm-level).

## 4. Preprocessing → `data/processed/atlas_v5_preprocessed.h5ad` (3k)
- Dropped **63,714 zero-count cells** → 4,016,176.
- **HVG flavor history:** `pearson_residuals` STALLED (>1 h); `seurat_v3` CRASHED
  (singular-matrix loess on a degenerate per-batch variance) → switched to
  **`cell_ranger`** (binned dispersion, no loess). n=3000, `batch_key=bio_sample`.
  `MIN_BATCH=50` prune = **no-op** (smallest bio_sample 162 cells).
- Parameterized `preprocess_atlas.py` (`--in/--out-tag/--n-hvg/--flavor/--force` +
  overwrite guard protecting prior outputs).

## 5. scVI → `data/scvi_model_v5_full/`, `data/scvi_latent_v5_full.h5ad` (4,016,176×30)
- **15 epochs** (batch 2048): clean monotonic ELBO 1294→1200.
- **100-epoch experiment** (batch 512): reconstruction flat (1124.1→1124.9), extra
  ELBO = pure KL shrinkage, validation reconstruction drifts UP → **15 epochs is
  adequate, not underfit**. Integration batch/bio ratio only 1.82→1.91 → residual
  batch structure is **structural** (505 `tech_sample` ≈ biology), not optimization.
  **Don't chase epochs**; levers are scANVI / coarser batch key / stratified metrics.

## 6. Validation (all PASS)
- Integrity: integer counts (max 55,140), lognorm X (max 8.85), 0 degenerate
  cells/genes, 505 batches (min 162 cells).
- Markers atlas-wide: SOX2 39%, VIM 73%, DCX 50%, MKI67 9.5%, RBFOX3 13%, GFAP 7.3%.
- **Per-deposit pan-markers present in all 118 deposits → no gene mis-alignment.**
- Outlier `gse290048_pineal` = coherent pineal biology (PDC 35%, RORB 49%); watch item.

## 7. GitHub + Codex review
- Pushed to **`nibafanfan/brain_org` `main`**: 15 pipeline scripts + `fetch_hnoca.sh`,
  11 docs, README (pipeline + reviewer guide), `v5_outcomes_and_validation.md`.
  Data tree + `*.xlsx` gitignored; 6 one-off scripts excluded.
- Codex review (`docs/method_review_2026-05-25.md`, merged from its pushed branch)
  **converged**: 15ep not underfit, structural confounding, recommends scANVI /
  coarser batch key / `seurat_hto_control` loader / parameterized paths.
- NOTE: most Codex commits are **stranded in its sandbox** (not pushed): c7b9e8e,
  7261d3a, 3d1c7e9, 87bd618, **41d3609** (collapse guardrail) are NOT fetchable;
  only its method-review branch tip `3f963ed` is on GitHub.

## 8. HVG 3k vs 5k + rare-lineage diagnosis
- 5k (`atlas_v5_5k_preprocessed.h5ad`, `atlas_v5_5k_hvg.tsv`) = near-superset of 3k
  (2997/3000) + endothelial PECAM1/KDR; **no microglia-specific markers at any
  depth**. 3k files untouched.
- Markers ARE expressed: microglia ~1% (P2RY12 1.0%, TMEM119 0.64%), endothelial
  ~1–3% (CLDN5 2.95%).
- **The 0% Immune/Vascular is class imbalance, NOT features**: Immune 0.5% /
  Vascular 0.7% of the Braun reference; pilot subsampling + argmax suppress them.
  Forcing markers into the transfer panel changed nothing (42/47 already present).

## 9. Braun → scANVI label transfer (`braun_label_transfer.py`)
- Added: **Region × organoid_type** cross-check (validated — Midbrain→Midbrain,
  Telencephalic→Telencephalon are top calls), `annotation_level`-stratified
  confidence, fixed CellClass cross-check to use `cell_type_original` (not the
  esc/ipsc cell line), `RARE_PANEL` insurance, soft-prob diagnostic.
- **Pilot** (200k, no panel): ~99% neural, conf 0.94, **0% Immune/Vascular**.
- **Pilot** (panel): still 0%; composition shifted (run-to-run noise).
- **FULL run** (3.2 h, all 1.67M ref + 4M query) → **CLASSIFIER COLLAPSE: 100%
  Radial glia, conf 1.000.** Region kNN is fine (Telencephalon 37%, Forebrain 18%…).
  Cause: scANVI classifier head collapsed during surgery (warning "Found 85 modules
  in eval mode"; flat degenerate surgery ELBO ~404). **Latent is usable; classifier
  prediction is not.**
- Outputs: `data/braun_transfer_full.h5ad` (4M query latent in X — usable),
  `data/braun_scanvi_full/` (reference model).

---

## CURRENT STATE / NEXT STEPS
1. **Fix the collapse via kNN-on-latent CellClass** (like Region, which worked).
   Cheap rescue: reuse `braun_transfer_full.h5ad` (query latent) + `braun_scanvi_full/`
   (model) → recompute reference latent, kNN-transfer CellClass. Validate by checking
   the kNN Region matches the full-run Region (~37% Telencephalon). **PENDING.**
2. **Re-implement Codex's collapse guardrail** (reference self-pred accuracy check +
   `--min-ref-acc` 0.60 / `--collapse-frac` 0.98 + query near-collapse warning) — it's
   stranded in Codex's sandbox, so re-implement from spec. Makes future runs fail-fast.
3. **Adopt kNN for CellClass** in `braun_label_transfer.py` (drop reliance on the
   collapsing scANVI classifier head); keep scANVI for the label-aware latent.
4. **`git push origin main`** — local commits ahead of remote include `f77c52a`
   (HVG/rare-lineage diagnosis + cross-checks + param preprocess) and the Codex
   method-review merge, plus this worklog.
5. Open items: `seurat_hto_control` loader formalization; Tier-2 GSM coverage
   (141 GSMs: 105 unbuilt accessions + 36 pooled/unsplit); parameterize remaining
   absolute paths.

## KEY FILES & LOGS
- Atlas: `data/atlas_v5_full.h5ad`
- Preprocessed: `data/processed/atlas_v5_preprocessed.h5ad` (3k),
  `data/processed/atlas_v5_5k_preprocessed.h5ad` (5k); HVG tables `data/atlas_v5*_hvg.tsv`
- scVI: `data/scvi_model_v5_full/`, `data/scvi_latent_v5_full.h5ad`
- Transfer: `data/braun_transfer_full.h5ad`, `data/braun_scanvi_full/`,
  pilots `data/braun_transfer_pilot{,_panel}.h5ad`
- Logs: `data/logs/{concat_v5*,preprocess_v5*,train_scvi_v5*,braun_*}.log`
- Run env: `/opt/homebrew/Caskroom/miniforge/base/bin/python3.13` (scvi 1.4.1, MPS)
